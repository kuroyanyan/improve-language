{
  "name": "bizmates-log",
  "private": true,
  "version": "1.0.0",
  "description": "Bizmates Log — 自分の話を英語で言えるようにする学習ログ（静的サイト・依存なし）",
  "type": "module",
  "scripts": {
    "serve": "python3 -m http.server 8787",
    "check": "node --check assets/app.js && node --check assets/ai.js && node --check assets/pieces.js && node --check assets/sample.js && node --check assets/sync.js && node -e \"JSON.parse(require('fs').readFileSync('assets/data/lessons.json','utf8'))\" && echo syntax OK",
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
